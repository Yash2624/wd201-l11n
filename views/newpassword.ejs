<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <link rel="stylesheet" href="./css/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Change Password</title>
  </head>

  <body>
    <div class="bg-gradient-to-r from-red-500 via-orange-500 to-purple-500 font-sans text-2xl">
      <div class="flex flex-row justify-between pr-4">
        <h1 class="w-3/5 p-5 text-white">Change Password</h1>
      </div>
    </div>
    <div class="flex flex-col items-center">
      <div class="h-1/6 text-black">.</div>
      <div class="w-1/3 shadow-xl p-4 mt-4 rounded-md bg-pink-200">
        <form action="/newpass" method="post">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <div class="py-3">
            <label for="currPass">Current Password</label>
            <input
              name="currPass"
              type="password"
              required
              class="rounded border-2 border-yellow-500 p-2 w-full text-white-900"
            />
          </div>
          <div class="py-3">
            <label for="newPass">New Password</label>
            <input
              name="newPass"
              type="password"
              required
              class="rounded border-2 border-yellow-500 p-2 w-full text-white-900"
            />
          </div>
          <div class="py-3">
            <label for="cnfPass">Confirm New Password</label>
            <input
              name="cnfPass"
              type="text"
              required
              class="rounded border-2 border-yellow-500 p-2 w-full text-white-900"
            />
          </div>
          <div class="py-3 flex flex-row">
            <button
              type="submit"
              class="py-2 px-6 rounded font-medium bg-red-500 hover:bg-red-600"
            >
              Change
            </button>
          </div>
        </form>
        <% if(messages && messages.error && messages.error.length > 0)  { %>
          <% for(var i=0; i<messages.error.length; i++) { %>
            <%- include('error', {message: messages.error[i]}) %>
          <% } %>
        <% } %>
      </div>
    </div>
  </body>
</html>
