<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <link rel="stylesheet" href="./css/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Admin Login</title>
  </head>

  <body>
    <div class="bg-gradient-to-r from-red-500 via-orange-500 to-purple-500 font-sans text-2xl">
      <div class="flex flex-row justify-between pr-4">
        <h1 class="w-3/5 p-5 text-white">Login to your admin account</h1>
      </div>
    </div>
    <div class="flex flex-col items-center">
      <div class="h-1/6 text-white">.</div>
      <div class="w-1/3 shadow-xl p-4 mt-4 rounded-md bg-voilet-200">
        <form action="/session" method="post">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <div class="py-3">
            <label for="email">Email</label>
            <input
              name="email"
              type="email"
              class="rounded border-2 border-yellow-500 p-2 w-full text-black-900"
            />
          </div>
          <div class="py-3">
            <label for="password">Password</label>
            <input
              name="password"
              type="password"
              class="rounded border-2 border-yellow-500 p-2 w-full text-white-900"
            />
          </div>
          <div class="py-3 flex flex-row">
            <button
              type="submit"
              class="py-2 px-6 rounded font-medium bg-yellow-400 hover:bg-yellow-500 text-white"
            >
              Sign In
            </button>
            <p class="py-2 px-4">New here? <a href="/signup" class="text-green-500">Sign-up</a></p>
          </div>
        </form>
        <% if(messages && messages.error && messages.error.length > 0)  { %>
          <% for(var i=0; i<messages.error.length; i++) { %>
            <%- include('error', {message: messages.error[i]}) %>
          <% } %>
        <% } %>
      </div>
    </div>
  </body>
</html>
